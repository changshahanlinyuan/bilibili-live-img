{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "python -m venv --copies /venv && . /venv/bin/activate && pip install -r requirements.txt && pip uninstall -y opencv_python"
    },
    "deploy": {
        "preDeployCommand": ["python -m pip install -r requirements.txt"],
        "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn bilibiliLiveImg.wsgi"
    }
}